## Added partial example

The API as it is used, documented, and taught via the tutorial is very, VERY
verbose.  The purpose of this example is to show how cut-down the API can
become if you really don't care about *what* the errors are, just if there
was one.
